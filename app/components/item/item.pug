include ~common/views/_mixins.pug

article
  header
    figure
      +image(image, "menu-item")
    h2.name= name
  p.price $#{parseFloat(price).toFixed(2)}
  footer
    a.add_cart(href="#") Add to cart
